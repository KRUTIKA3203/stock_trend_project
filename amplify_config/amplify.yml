version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - echo "Building PHP site..."
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
